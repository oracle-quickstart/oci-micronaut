kubernetes:
  client:
    discovery:
      mode: service
      enabled: true
      mode-configuration:
        endpoint:
          watch:
            enabled: true
        service:
          watch:
            enabled: true
