datasources:
  default:
    ocid: ${ORACLE_CLOUD_ATP_OCID}
    walletPassword: ${ORACLE_CLOUD_ATP_WALLET_PASSWORD}
    username: ${ORACLE_CLOUD_ATP_USERNAME}
    password: ${ORACLE_CLOUD_ATP_PASSWORD}
tracing:
  zipkin:
    enabled: true
    sampler:
      probability: 1
    http:
      url: ${ORACLECLOUD_TRACING_ZIPKIN_HTTP_URL}
      path: ${ORACLECLOUD_TRACING_ZIPKIN_HTTP_PATH}
micronaut:
  metrics:
    enabled: true
    export:
      prometheus:
        enabled: true
        descriptions: true
        step: PT1M
      oraclecloud:
        enabled: false
        namespace: ${ORACLECLOUD_METRICS_NAMESPACE:micronaut-mushop}
        resourceGroup: ${ORACLECLOUD_METRICS_RESOURCEGROUP:mushop-catalogue}