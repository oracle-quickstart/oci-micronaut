kubernetes:
  client:
    discovery:
      mode: service
      enabled: true
      services:
        users:
          name: mushop-user
        carts:
          name: mushop-carts
      mode-configuration:
        endpoint:
          watch:
            enabled: true
        service:
          watch:
            enabled: true
